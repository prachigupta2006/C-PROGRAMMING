#include <stdio.h>

int main()
{    
    int a=-10 ;
    if (a<0)
       {
           printf("the number %d is positive ",a);
           a=a+5;
       }
    else
       {
           printf("the number %d is negative ",a);
           a=a-5;
       }
    printf("\n\n%d",a);
    return 0;
}